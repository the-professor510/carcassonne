#pragma once

class State {
    // things to have!
    //      - disjoint-set data structure for tracking connected zones.
    //      - "head" node for the board, that we can add to and move in from
    //      - perhaps keep track of a "border" to make move generation simpler
};